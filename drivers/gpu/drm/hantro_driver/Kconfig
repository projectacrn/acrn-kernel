config DRM_HANTRO
	tristate "Hantro Driver"
	depends on DRM
	default m
	select DRM_PANEL
	select DRM_KMS_HELPER
	help
	  Choose this option if your system supports Hantro Driver
	  for KMB ARM Media. This driver supports video and JPEG
	  encode/decode.
